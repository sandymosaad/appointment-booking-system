export interface slotData  {
    id?: string;
    date: string;
    start_time: string;
    end_time: string;
    provider_id: string;
    provider_name: string;
    status?: string;
    client_id ? :string;
    expires_at?:string;
}
